# 书签清理助手 · 方案文档（v1 已确认 · 2026-09-20）

> 状态：**已确认，动工中**（P1-P3 全做）
> 定位：公益免费扩展（不盈利）· 积累扩展开发经验 · 潜在未来渠道
> 纪律：local-only、零服务器、零客服、周末级时间盒
>
> **确认记录（2026-09-20）**：① 名称=书签清理助手 ② 范围=P1-P3 全做
> ③ 分工=A（AI 写 MVP，用户验证学习）④ 仓库=公开 + MIT ⑤ 社交功能（P4）=规模起来前不做

---

## 0. 命名候选（待定）

| 候选 | 优点 | 缺点 |
|---|---|---|
| **书签清理助手** | 最直白；商店搜索"书签清理"直接命中 | 偏窄（只体现"清理"） |
| 智能书签整理 | 覆盖清理+整理 | "智能"两字滥用严重 |
| 书签整理大师 | 大气 | "大师"感营销味重 |
| 书签医生 Bookmark Doctor | 诊断+救援隐喻（契合搬家检测/Wayback 救援）| 不含"清理"搜索词 |
| 书签管家 Bookmark Butler | 持续管理感 | 同上 |

**商店搜索提示**：扩展的发现渠道 = 商店搜索（V2EX 案例验证）。名称含"书签清理"直接吃搜索词。
**倾向建议**：主名 `书签清理助手`，副标题覆盖全部能力（清理 · 归档 · 救援 · 画像）。

---

## 1. 定位与目标

| 项 | 内容 |
|---|---|
| 用户 | 书签囤积党（评论实证：有人删过 1 万多条重复书签） |
| 价值 | 让书签"能用"：清理死链、去重、归档、救援、可视化 |
| 商业模式 | 无（公益）；远期若规模起来，作为产品/站点渠道 |
| 成功标准 | 自己每天用 + 商店自然搜索有安装量（不做推广） |
| 失败标准 | 维护成本超时间盒（≤2h/周）或 3 个月无人用 |

## 2. 功能清单

### P1 — MVP（覆盖懒猫 + 安全性升级）

| 功能 | 说明 | 懒猫对比 |
|---|---|---|
| 失效书签检测 | 并发链接检查（超时/状态码/重定向分类） | ✅ 同类 |
| 重复书签检测 | URL 归一化（去 www/尾斜杠/utm 参数）+ 哈希 | ✅ 同类 |
| 空文件夹检测 | 书签树遍历 | ✅ 同类 |
| 列表 UI + 筛选 | 分类展示、逐项勾选、预览 | ✅ 同类 |
| 删除 | 批量删除选中项 | ✅ 同类 |
| **删除前自动备份** | 导出完整书签 JSON（一键恢复） | ⭐ 升级（懒猫警告"无法恢复"） |
| **回收站 + 撤销** | 删除项进本地回收站（chrome.storage），30 天内可恢复 | ⭐ 升级（安全性是最大差异点） |

### P2 — 整理能力

| 功能 | 说明 |
|---|---|
| **移动/归档** | 选中项 → 移动到指定文件夹；一键"死链归档"自动建文件夹 |
| 批量打开 | 选中项一键批量打开（分批限流，防炸浏览器） |
| 书签画像 | 域名分布 Top、死链率、重复率、月度新增趋势、收藏时间线 |
| 报告导出 | 扫描结果导出 HTML/CSV（可分享的清理报告） |

### P3 — 救援能力（核心差异化）

| 功能 | 说明 |
|---|---|
| **搬家站检测** | 跟随重定向：域名变了 → 标记"已搬家"，一键更新为新地址 |
| **Wayback 救援** | 死链查 archive.org 快照：有存档 → 一键替换为存档地址（死链复活） |
| 批量分享书签 | 选中项导出为自包含 HTML 页面（精美书签清单，可直接分享） |

### P4 — 远期（需单独评估，暂缓）

| 功能 | 说明 | 顾虑 |
|---|---|---|
| 书签社交（"多少人和你收藏了同一页"） | 需服务器 + 聚合计数 | ⚠️ **破坏 local-only**；冷启动死穴（没用户=计数无意义）；隐私设计（只传 URL 哈希）；依赖规模，**MVP 不做** |

## 3. 技术方案

```
Manifest V3 扩展（纯前端，无服务器无账号）
├── background (service worker)   链接检测队列（并发 8-10，超时 8s，HEAD→GET 降级）
│                                 重定向追踪（fetch 后比对 response.url）
│                                 Wayback 查询（archive.org availability API）
├── dashboard (全页 UI)           扫描/列表/画像/回收站（核心界面）
├── popup (弹窗)                  快捷入口：一键扫描 + 打开仪表盘
└── storage (chrome.storage.local) 设置 / 回收站 / 扫描缓存 / 统计
```

- **语言/构建**：TypeScript + Vite（最小依赖，产出即扩展目录）
- **权限**：`bookmarks` + `storage` + `host_permissions`（链接检测需要；考虑改可选权限按需申请，降低审核阻力）
- **i18n**：中文 + English（chrome.i18n）
- **无网络数据外发**：除目标书签 URL 本身（检测必然访问）和 archive.org 查询外，零上传
- **浏览器兼容**：Chrome/Edge 优先（同一 MV3 代码），Firefox 后置

## 4. 阶段计划（时间盒）

| 阶段 | 内容 | 预估 |
|---|---|---|
| P1 | 骨架 + 扫描 + 列表 UI + 删除 + 备份/回收站 | 1-2 个周末 |
| P2 | 移动归档 + 批量打开 + 画像 + 报告导出 | 1 个周末 |
| P3 | 搬家检测 + Wayback 救援 + 分享导出 | 1 个周末 |
| P4 | 中英双语打磨 + 商店上架准备（图标/截图/隐私说明） | 半天 |
| P4+ | 社交功能 | 仅当用户规模起来后单独评估 |

**实现状态（2026-09-20）**：P1-P3 已实现（v0.1.0，零构建原生 ESM，待 Chrome 实测验证）：
- P1：扫描（失效/重复/空夹）+ 列表 UI + 删除 → 回收站（可恢复）+ 导出备份 ✅
- P2：移动/归档 + 死链一键归档 + 批量打开 + 书签画像 + CSV 报告 ✅
- P3：搬家检测（重定向域名比对）+ Wayback 查询/替换 + 分享页导出 ✅
- 已知限制：万级书签全量扫描耗时较长（并发 8）；MV3 SW 长任务可能被系统回收（状态已持久化，可重扫）

## 5. 差异化总结（vs 懒猫）

| 能力 | 懒猫 | 我们 |
|---|---|---|
| 清理（死链/重复/空夹） | ✅ | ✅ |
| 删除不可恢复 | ⚠️（提示备份但无功能） | ✅ 自动备份 + 回收站撤销 |
| 只能删除 | ❌ | ✅ 移动/归档 |
| 搬家站检测 | ❌（评论明确抱怨） | ✅ |
| Wayback 救援 | ❌ | ✅ |
| 批量分享 | ❌ | ✅ |
| 画像 | ✅ | ✅（增强：死链率/趋势） |
| 隐私 | ✅ local | ✅ local |
| 语言 | 中英 | 中英 |

## 6. 风险与边界

1. **链接检测的工程坑**：部分站点屏蔽 HEAD/爬虫；大书签量（1 万+）检测耗时长 → 并发控制 + 断点续扫 + 结果缓存
2. **MV3 service worker 生命周期**：长任务会被挂起 → 分片处理 + alarms 唤醒
3. **审核**：`host_permissions` 全站权限会引来审查 → 准备隐私说明；可选权限按需申请
4. **受众与渠道错配**（已记 idea-log）：中文扩展用户 ≠ 英文数据站受众；渠道价值要等目标匹配的产品
5. **公益边界**：不接客服、不承诺 SLA；Issues 处理有节奏

## 7. 待确认问题

1. **名称**：书签清理助手（推荐）还是其他？
2. **范围**：P1-P3 全做，还是先 P1 验证手感？
3. **代码分工**：A. 我写 MVP 你验证学习 / B. 你写核心我出规格+评审？
4. **仓库**：GitHub 公开（MIT）？目录 `~/lab/gefei_tools/bookmark-doctor/`？
5. **社交功能（P4）**：确认"规模起来前不做"？
